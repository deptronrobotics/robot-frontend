import type {Config} from "prettier"

const config: Config = {
    trailingComma: "all",
    tabWidth: 4,
    semi: true,
    quoteProps: "as-needed",
    bracketSpacing: true,
    objectWrap: "collapse",
    bracketSameLine: false,
    arrowParens: "always",
}

export default config;